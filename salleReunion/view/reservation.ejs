<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Réserver la salle</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('/IMG_4185-1024x683.jpg');
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }
        .container {
            max-width: 400px;
            margin: 0 auto;
            margin-top: 50px;
            background-color: #1f1616;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(48, 45, 45, 0.1);
        }
        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #007bff;
            animation: colorChange 5s infinite alternate;
        }
        @keyframes colorChange {
            0% { color: #007bff; }
            100% { color: #a72837; }
        }
        .navbar {
            background-color: #000000 !important;
            height: 40px; /* Réduire la hauteur de la navbar */
        }
        .navbar-brand {
            color: #fff !important;
            font-weight: bold;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .btn-primary {
            width: 100%;
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }
    </style>
</head>
<body>
    <!-- Barre de navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Reservation Salle de Reunion</a>
        <!-- Ajoutez ici d'autres éléments de la barre de navigation si nécessaire -->
    </nav>

    <!-- Contenu principal -->
    <div class="container mt-4">
        <h1>Réserver la salle</h1>
        <% if (salle) { %>
            <div class="alert alert-info" role="alert">
                <p>Informations sur la salle :</p>
                <ul>
                    <li><strong>Nom de la salle :</strong> <%= salle.name %></li>
                    <li><strong>Capacité :</strong> <%= salle.capacity %></li>
                    <li><strong>Emplacement :</strong> <%= salle.location %></li>
                    <li><strong>Prix par jour :</strong> <%= salle.prix %> Dinars</li>
                   
                </ul>
            </div>
        <% } %>
        <% if (successMessage) { %>
            <div class="alert alert-success" role="alert">
                <%= successMessage %>
            </div>
        <% } %>
        <% if (errorMessage) { %>
            <div class="alert alert-danger" role="alert">
                <%= errorMessage %>
            </div>
        <% } %>
        <form action="/reservations/reserve" method="POST">
            <div class="form-group">
                <label for="dateDebut">Date de début :</label>
                <input type="date" class="form-control" id="dateDebut" name="dateDebut" required>
            </div>
            <div class="form-group">
                <label for="dateFin">Date de fin :</label>
                <input type="date" class="form-control" id="dateFin" name="dateFin" required>
            </div>
            <input type="hidden" name="salleId" value="<%= salleId %>">
            <button type="submit" class="btn btn-primary">Réserver</button>
        </form>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
